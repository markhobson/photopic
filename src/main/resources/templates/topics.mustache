<!DOCTYPE html>
<html>
	{{>head}}
	<body>
		{{>navbar}}
		<div class="container">
			<div class="page-header">
				<h1>Topics</h1>
			</div>
			{{#has-topics}}
				<p>The following photo topics are available:</p>
				<table class="table">
					{{#topic}}
						<tr>
							<td style="width: 100%">{{name}}</td>
							<td>
								<form method="POST" action="/topic/{{id}}">
									<input type="hidden" name="_method" value="DELETE"/>
									<button type="submit" class="btn btn-default">
										<span class="glyphicon glyphicon-trash"></span>
									</button>
								</form>
							</td>
						</tr>
					{{/topic}}
				</table>
			{{/has-topics}}
			{{^topic}}
				<p>There are no photo topics yet. Why not add one?</p>
			{{/topic}}
			<a href="/topics/create" class="btn btn-primary">
				<span class="glyphicon glyphicon-plus"></span> Add a topic
			</a>
		</div>
	</body>
</html>
